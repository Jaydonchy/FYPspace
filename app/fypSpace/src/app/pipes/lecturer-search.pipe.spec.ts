import { LecturerSearchPipe } from './lecturer-search.pipe';

describe('LecturerSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new LecturerSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
